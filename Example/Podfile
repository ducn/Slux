source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'Slux_Example', :exclusive => true do
  pod 'Slux', :path => '../'
end

target 'Slux_Tests', :exclusive => true do
  pod 'Slux', :path => '../'

  pod 'Quick', '~> 0.8.0'
  pod 'Nimble', '3.0.0'
  pod 'FBSnapshotTestCase'
  pod 'Nimble-Snapshots'
end
